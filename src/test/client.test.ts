import {Client} from '../client';
let client: Client;
beforeEach(() => {
  client = new Client('API_KEY');
});

describe('Client', () => {
  test('get should throw when response is ok',async() => {});
});
